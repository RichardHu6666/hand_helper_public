dependencies:
  idf:
    version: ">=5.3.0"
  lvgl/lvgl: "9.2.2"
  esp_lcd_ek79007: "^1.0.0"
  esp_lcd_ili9881c: "^1.0.0"
  espressif/esp_cam_sensor: "^1.2.1"
  espressif/hand_detect: ">=0.1.0"
  espressif/hand_gesture_recognition: ">=0.1.0"
  espressif/esp-dl: ">=3.1.0"
  espressif/esp_wifi_remote:
    version: ">=0.10,<2.0"
    rules:
      - if: "target in [esp32p4, esp32h2]"
  espressif/esp_hosted:
    version: "~2"
    rules:
      - if: "target in [esp32p4, esp32h2]"
  espressif/esp-extconn:
    version: "~0.3.0"
    rules:
      - if: "target in [esp32p4]"

